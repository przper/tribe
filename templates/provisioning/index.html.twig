{% extends 'base.html.twig' %}

{% block body %}
    <div class="content">
        <h1>Listy zakupów</h1>

        <div>
            <ol>
                {% for list in groceryLists %}
                    <li>
                        <a href="{{ path('provisioning.groceries.shopping', {id: list.id}) }}">{{ loop.index }}</a>
                    </li>
                {% else %}
                    <div>No groceries to do</div>
                {% endfor %}
            </ol>
        </div>
    </div>
{% endblock %}
